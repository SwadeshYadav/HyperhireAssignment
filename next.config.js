/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['encrypted-tbn0.gstatic.com', 'example.com'], // Add the domains you want to allow
    },
  };
  
  module.exports = nextConfig;
  